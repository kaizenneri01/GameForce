import { Box } from "@mui/system";
import styed from "@emotion/styled";
import ImageOne from "../../Asset/images/ResidentEvil8.jpg";

const NavLogo = styed(Box)(() => ({
  height: "3.5rem",
  width: "6rem",
  padding: ".5rem",
}));

export default NavLogo;
